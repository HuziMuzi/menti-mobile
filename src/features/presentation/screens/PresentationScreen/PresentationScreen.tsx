import React from 'react';

import PresentationStack from '../../navigation/presentationStack';

const PresentationScreen = () => {
  return <PresentationStack />;
};

export default PresentationScreen;
